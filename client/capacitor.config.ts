import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.wardrobemanagement.app',
  appName: 'Wardrobe Management',
  webDir: 'dist'
};

export default config;
